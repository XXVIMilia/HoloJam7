using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerCrouchingState : PlayerMovementState
{
    public PlayerCrouchingState(Player player, PlayerMovementStateMachine playerMovementStateMachine) : base(player, playerMovementStateMachine)
    {
    }

    //Overrides will be generated after regeneration and adding into the unity project

}